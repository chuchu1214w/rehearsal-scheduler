# 开发日志

> 按时间记录每次开发做了什么、为什么这么做、遇到什么问题怎么解决、下一步是什么。
> 详细需求与设计见《开发文档》《交互设计》,上线操作见《部署指南》;本日志只记过程与决定。

## 2026-09-21(周一)· 从需求到上线一天完成

**00:46–01:22 需求确认**
- 依据《时间调度代码要求.docx》与 Colab 原型 `untitled11.py`,通过多轮提问确认需求,写成《开发文档》v0.1 → v1.0。
- 关键决定:每天 10:00–23:00 统一窗口(去掉周末 / 末班车规则);公演前一天只排 1 场全员评估(2–3h,允许迟到早退);去掉「困难曲目尽早」;单日软 / 硬上限都 8h;部署先本机、以后选最便宜的云。

**01:55 M0 求解包**
- 把原型重构为独立的 `backend/solver` 包(CP-SAT 字典序优化、缺席降级阶梯、评估场穷举、无解诊断、独立校验器)+ 命令行。70 个测试。

**02:30–06:25 M1 / M2 与前端重做**
- 第一版用 antd 的界面被评价"丑、看不懂名册页",于是先做《交互设计》(四步向导、七步工作台、9 项决定),再按 ChatGPT 生成的手机 UI 草图整体重做前端(移除 antd,自写组件,移动优先 + 桌面宽屏)。
- 后端:管理员开通账号(不用邀请链接)、填报截止、7 种特殊要求填空句、涂格填报、热力图、代填、求解前检查。
- 用户看不了 Markdown,所有文档改为 PDF 交付(`scripts/md2pdf.py`,无头 Chrome)。

**06:59 M3 一键求解与诊断**
- 求解在独立子进程,任务表记录进度,前端 1.5 秒轮询;无解时给一句话结论 + 每曲缺口 / 最小调整建议 / 只差一人时段 / 评估场。
- 数据库结构版本 2→3 做成自动迁移,用户的库不用删。
- 演示数据实测:无解 → 按建议补空闲 → 4 秒得到 L1 层级最优解。

**07:43 M4 排练表**
- 课程表式周日历、点日期进当天日程、成员端「我的 / 全体」切换、发布 / 撤回、ICS 日历订阅(私密令牌)。
- 问题:PDF 里多张流程图重叠。原因是无头 Chrome 虚拟时间下 `Date.now()` 不走,Mermaid 给多张图同一个 id。改为逐张显式 id + 两步渲染(先静态化再打印)后解决。
- 问题:ICS 行折叠按字节切开了多字节中文 → 改为按字符切。

**08:08 M5 调整能力**
- 拖动移动(校验器把关、新引入硬冲突就弹回并说明原因)、锁定与「锁定后重排」、版本对比、发布后成员改空闲的冲突提示。
- 决定:草稿原地改;发布 / 归档版本改动时自动复制成新草稿;时长不可拉伸(由曲目方案决定)。

**08:24 M6 通知与打磨**
- 站内通知中心(7 类触发)、后台定时提醒、一键催办改为站内通知、优化优先级面板、规则冲突检查、端到端自动化测试。125 个测试。

**12:54 M7 部署材料**
- Dockerfile、fly.toml、docker-compose + Caddy、`/api/health`、每日自动备份、《部署指南》。选型改为 Fly.io 优先(约 $3.3/月),Oracle 免费主机为 $0 备选。

**17:07 上线**
- 用户注册 Fly.io 并授权 CLI;创建应用 `timetomeet`、东京 1 GB 数据盘;远程构建约 7 分钟(其中等 builder 排队 6 分钟)。
- https://timetomeet.fly.dev 上线,健康检查通过。

**17:37 排练地点**
- 管理员在场次弹窗填地点;已发布版本立即通知该场成员;显示在详情 / 列表 / 当天 / 首页 / 日历。结构版本 6。
- 事故:`git add -A` 带上了 Word 临时锁文件 `~$…docx`(含编辑者姓名)并推到了公开仓库;已用新提交删除并加入 `.gitignore`,历史提交里仍有,是否改写历史待用户决定。教训:`git add -A` 前先看 `git status`。
- 事故:项目目录被整个移进了一个同名子文件夹(原因不明,疑似 Finder 操作),已移回原路径。

**18:59 PWA 与手机推送**
- 用户问"小程序还是上架 App":结论先做 PWA(不用备案、不用审核、$0);做了 manifest / 图标 / Service Worker,Web Push(VAPID 自动生成、订阅表、站内通知同步推送),账号页 / 首页 / 通知页的安装与推送引导。130 个测试。

## 2026-09-26(周六)· 原生 iOS App

**决定**
- 用户已购 Apple Developer Program;团员基本用 iPhone;分发两步走:先 TestFlight,再上架 App Store。
- 本机 Xcode 27 已装并登录开发者账号(Team ID `DP58FN46L6`),无 CocoaPods / fastlane。

**进行中**
- 4 个方向并行调研(苹果审核与 TestFlight 规则、Capacitor 打包、后端 APNs、替代路线),核对后出方案。
- 已生成 1024 px 图标与 2732 px 启动图源文件(`resources/`)。
- 用户已创建 APNs Auth Key(Key ID `N43HW9YZ26`,文件放在 Downloads,不进仓库)与 App Store Connect 的 App 记录;Team ID `DP58FN46L6`;Bundle ID 拟用 `app.timetomeet.season`。
- 用户提出:App 开发全程要写开发日志(本文件)。

**方案(调研 9 个代理、1 小时)**
- Apple 审核指南 4.2 明确反对"重新打包的网站";`server.url` 指线上被官方标为非生产用途 → 采用「混合方案」:前端构建产物打进 App,API 指向 https://timetomeet.fly.dev。
- WKWebView 里 `rs_session` 是第三方 Cookie(iOS 14 起 ITP 默认拦)→ 登录改为令牌:请求头 `X-Client: native` 时登录接口额外返回令牌,存 Preferences,之后 `Authorization: Bearer`;后端 Cookie / Bearer 两种都认。
- Web Push 在 WKWebView 不可用 → 原生推送走 APNs 直连(HTTP/2 + .p8 JWT,httpx + PyJWT,不引 Firebase);站内通知同时扇出到 Web Push 与 APNs。
- 分发:TestFlight 先用;上架审核通过后申请 Unlisted(仅链接可见,不再有 90 天过期)。Capacitor 8.5.2 + SPM(Xcode 27 要求 ≥ 8.5)。

**实现**
- 后端:`session_token_from_request`(Cookie 或 Bearer)、登录 / 首次设置返回令牌、CORS 白名单(capacitor://localhost 等)、`/.well-known/apple-app-site-association`(Universal Links)、`native_push_tokens` 表(结构版本 8)、`app/apns.py`、`push.send_to_users` 扇出原生;新增 6 个测试,共 136 个。
- 前端:`src/native.ts`(令牌、原生推送注册 / 注销、深链接与点通知跳转)、`api/client.ts` 支持 API 前缀与 Authorization、原生环境不注册 Service Worker、账号页在 App 内显示「推送通知」开关;`capacitor.config.ts`;`npm run build:app`(VITE_API_BASE 指线上)。
- iOS 工程:`frontend/ios`(Capacitor 8.5.2,SPM,SceneDelegate);图标 / 启动图由 `@capacitor/assets` 从 `resources/` 生成;entitlements(aps-environment、applinks:timetomeet.fly.dev);Team ID 写入工程;AppDelegate 加 APNs 注册回调;仅竖屏;`ITSAppUsesNonExemptEncryption = false`。
- 问题:测试里同步投递时清理失效 token 用了新会话,与请求事务在 SQLite 上撞写锁 → 同步模式改用请求自己的会话。
- 问题:用 `sh -c` 包命令时,heredoc 里的反引号被外层 shell 当作命令执行,把这段日志写坏了一次(已重写)。教训:heredoc 分隔符要加引号,或用 Python 写文件。
- APNs 密钥与 Team / Bundle ID 以 Fly secrets 配置并重新部署线上。

**模拟器验证**
- 用 `xcrun simctl` 把 Debug 包装进 iPhone 17(iOS 27)模拟器(内置的模拟器面板需要用户本人授权,改用命令行)。第一次全屏白 / 灰:排查了 WebKit 日志、注入了 JS 报错捕获与"信标"请求,最后发现是模拟器处于深色外观且截图恰好落在启动过渡,切到浅色外观后登录页正常显示。为稳妥起见:`<meta name="color-scheme" content="light">` + CSS `color-scheme: light`,让 WebView 不按系统深色模式渲染;启动图集里删掉模板遗留的 3 张未分配图片,深色启动图与浅色相同。
- 发现并修:登录页没算安全区,状态栏压住 logo → `.auth` 加 `safe-area-inset-top`;原生里不自动聚焦输入框(一打开就弹键盘);系统栏样式改为深色文字(`SystemBars.style = LIGHT`)。
- 线上已重新部署(v5,含 APNs 密钥与 CORS / AASA):`/.well-known/apple-app-site-association` 返回 `DP58FN46L6.app.timetomeet.season`,`OPTIONS /api/me` 对 `capacitor://localhost` 返回允许头。
- 未能在模拟器里点按登录(需要用户授权模拟器面板);令牌登录逻辑靠后端测试(`test_native.py`)与网页同源代码保证,真机验证留给用户。

**上传 TestFlight**
- `scripts/ios-release.sh`:`build:app`(API 指线上)→ `cap sync` → `xcodebuild archive`(自动签名)→ `-exportArchive` 上传。
- **卡住**:Archive 报 `No Account for Team "DP58FN46L6"` / 找不到 `app.timetomeet.season` 的描述文件。关掉命令沙箱重试仍然一样。检查发现:钥匙串里有 Team DP58FN46L6 的开发证书,但 Xcode 里登录的账号没有拉到任何团队(`IDEProvisioningTeams` 为空),本机唯一的描述文件属于另一个团队 N8P9A47FFG(个人免费团队)。命令行没法替用户登录 Apple ID,只能等用户:① 在 Xcode → Settings → Accounts 重新登录并确认能看到 DP58FN46L6 团队;或 ② 给我一把 App Store Connect API 密钥(脚本已支持 `ASC_KEY_PATH` / `ASC_KEY_ID` / `ASC_ISSUER_ID`)。
- 期间用量额度用尽一次(审查工作流 5 个代理全部失败),额度恢复后重跑。

**代码审查(第二轮,5 个方向 + 逐条反驳核实,约 50 个代理)**
- 核实为真、已修的主要问题:
  - 发布脚本:Archive 失败被 `|| true` 吞掉且不清旧包 → 可能把上一次的旧包当新包上传;macOS 自带 bash 3.2 下空数组 + `set -u` 直接报错。已重写:失败即停、每次清理、bash 3.2 兼容。
  - `capacitor.config.ts` 语法错误(逗号被写进行尾注释,靠 TS 容错才没炸)。
  - 登出不解绑本机推送 → 登出后这台手机仍收到原账号的通知。现在登出前先在服务器上解绑(网页推送同理),下个账号登录时自动重新绑定。
  - 冷启动不重新注册推送(APNs token 可能变化)→ 每次启动 / 换账号都重新注册;开启推送改为「拿到 token 且上报成功」才算开启。
  - 会话令牌存 UserDefaults(会进备份)→ 自写钥匙串插件 `SecureStore`(`kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`,不随备份迁移),旧令牌自动迁移;钥匙串异常时退回原方式保证能登录。
  - 会话只有滑动续期 → 加 90 天绝对寿命(`SESSION_MAX_DAYS`)。
  - APNs 非 410 错误被静默吞掉 → 记日志;`ExpiredProviderToken` 自动重签重试;启动时校验密钥。
  - 沙箱 / 生产环境错配会把合法 token 当失效删掉 → 每个 token 记住环境(`native_push_tokens.env`,结构版本 9),未知时先试默认环境、BadDeviceToken 再试另一个。
  - 同一台 iPhone 装了 App 又开过 Safari 网页推送会弹两次 → 装了 App 的人不再发 iPhone 网页推送。
  - AASA `paths: ["*"]` 会把日历订阅链接 `/cal/*.ics` 劫持进 App → 排除 `/api/*`、`/cal/*`、`/assets/*` 等。
  - 生产 CORS 白名单带着 localhost 开发来源 → fly.toml 只放 `capacitor://localhost`。
  - 其他:首次设置加锁防并发、登录失败记录用户名截断、APNs collapse-id、前台收到推送即刷新通知、会话失效(401)自动回登录页、断网时显示「重试」而不是登录页、登录后跳回原页面保留 `?event=`、iPhone 专用(不声明 iPad)、原生层强制浅色、清理模板残留。
- 未改(低风险、记录在案):登录限流按用户名计数可被用来锁定他人账号;每次推送新建 HTTP/2 连接。
- 后端 138 个测试通过。

**模拟器端到端自检**
- 在打进 App 的页面里临时注入自检脚本(不入库),在真实 WebView 里跑:钥匙串存 / 取 / 删 ✓、原生登录拿令牌 ✓、Bearer 调 `/api/me` ✓、推送 token 注册 / 注销 ✓、登出后令牌立即 401 ✓。
- 自检抓到两个问题:① Capacitor 8.5 模板在 `SceneDelegate` 里直接 new `CAPBridgeViewController`,不走 storyboard,导致自定义插件没注册 → 改为 `MainViewController()`;② 桌面文件夹由 iCloud 文件提供程序托管,构建产物被加上附加属性导致签名失败("resource fork, Finder information, or similar detritus not allowed")→ 构建产物、archive、export 全部放到 `~/Library/Developer/Xcode` 下。

**上传 TestFlight**
- 用户提供了 App Store Connect API 密钥(Key ID `L7S3ZB5MA2`,文件在 Downloads,不进仓库)与 Issuer ID。
- **事故:Team ID 用错了**。之前从钥匙串证书名「Apple Development: …(DP58FN46L6)」里读出 `DP58FN46L6` 当成团队 ID,写进了工程签名、导出配置、线上 `APPLE_TEAM_ID`(APNs JWT 的 iss 与 Universal Links 的 appID)。实际上括号里是个人成员标识,证书的 OU 字段和 App Store Connect API(bundleIds 的 seedId)都显示团队 ID 是 **`N8P9A47FFG`**。已全部改正并重新部署。教训:团队 ID 以苹果接口 / 开发者网站为准,不要从证书名里猜。
- **事故:iCloud「优化存储」**。桌面文件夹由 iCloud 托管,项目里约 1.3 万个文件(`.venv`、`node_modules`、甚至部分源码)被挪到云端只留占位,构建时 rollup 的原生模块读不到(ERR_DLOPEN_FAILED)。改为从 GitHub 克隆到 `~/Developer/season-release`(不在 iCloud 里)打包上传;建议用户把项目移出桌面或对项目文件夹选「保留下载」。

- 改正 Team ID 后签名通过:API 密钥让 Xcode 自动创建了开发描述文件;第一次签名时 macOS 弹出钥匙串对话框,用户输入开机密码并点「始终允许」后 Archive 成功。
- 导出失败「Cloud signing permission error」:这把 API 密钥不是管理员角色,不能云端签名。改为用 API 直接创建 **Apple Distribution 证书**(有效至 2027-09-26)和 **App Store 描述文件「Season App Store」**,证书私钥导入本机钥匙串,新增 `ios/ExportOptions-manual.plist` 手动签名导出。发布脚本默认改走手动签名。
- **上传成功**:构建 `202609270036`,苹果处理后状态 VALID;`ITSAppUsesNonExemptEncryption=false` 使加密合规自动免除。用 API 建了 TestFlight 内部测试组「舞团内部」(自动包含所有新构建)并写好「测试内容」说明。
- 待用户:在 TestFlight 内部测试组里把自己加为测试员 → iPhone 装 TestFlight → 安装 Season;真机验证登录、推送(生产 APNs)、深链接。外部测试(公开链接给团员)需要提交 Beta 审核,要填联系方式和审核用演示账号,由用户决定。

_(后续条目在开发过程中随时追加)_
