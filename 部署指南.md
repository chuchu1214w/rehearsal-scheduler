# 部署指南(M7)

> 目的:让成员用手机在任何网络下都能登录、填空闲、看排练表、订阅日历。
> 你不需要懂服务器:照着下面的命令一步步敲就行。整个过程大约 30 分钟。
> 两条路线任选其一:**方案 A Fly.io**(最省事,每月约 $2–4)/ **方案 B 自己的云主机**(可做到 $0,但步骤多、要自己维护)。

## 0. 先决定:选哪条路线

| | 方案 A · Fly.io | 方案 B · 云主机(Oracle Always Free 等) |
|---|---|---|
| 费用 | 常驻一台 shared-cpu-1x / 512 MB 约 **$3.3 / 月**,再加 1 GB 磁盘约 $0.08(新账号只有短暂试用,之后按量计费,需绑信用卡) | Oracle Always Free 为 $0(需绑信用卡验证;闲置实例可能被回收;免费额度 2026 年已下调),其他厂商最便宜的小主机约 $4–6 / 月 |
| 需要做的 | 装一个命令行工具,敲 6 条命令 | 申请主机 → 装 Docker → 申请域名 → 配置 HTTPS → 跑容器 |
| HTTPS / 域名 | 自动,给你一个 `xxx.fly.dev` 域名 | 要自己弄一个域名(可用免费的 DuckDNS),Caddy 自动申请证书 |
| 备份 | 服务每天自动备份到磁盘;再用一条命令拉回电脑 | 同左 |
| 建议 | **先用这个** | 想做到 $0、且愿意折腾时再换 |

价格来源(2026-09):[Fly.io 官方价格页](https://fly.io/pricing/)、[Fly.io 资源定价文档](https://fly.io/docs/about/pricing/)。开通前以官网为准。

两条路线用的是同一个 `Dockerfile`,以后互相迁移只需要「重新部署 + 恢复备份」。

---

## 方案 A · Fly.io(推荐)

### A1. 注册并安装命令行工具

1. 在 <https://fly.io> 注册账号,绑信用卡(不绑不能建应用)。
2. 在你的 Mac 终端安装工具并登录:

```bash
curl -L https://fly.io/install.sh | sh
```

装完按提示把 `~/.fly/bin` 加进 PATH(或重开一个终端),然后:

```bash
fly auth login
```

### A2. 改一下应用名

打开仓库根目录的 `fly.toml`,把两处 `season-rehearsal` 改成你自己的名字(全球唯一,只能小写字母、数字、短横线),例如 `season-dance-2026`:

```toml
app = "season-dance-2026"
PUBLIC_BASE_URL = "https://season-dance-2026.fly.dev"
```

### A3. 创建应用与数据盘(只做一次)

在仓库根目录执行(把名字换成你改的):

```bash
fly apps create season-dance-2026
```

```bash
fly volumes create data --region nrt --size 1 --app season-dance-2026 --yes
```

`nrt` 是东京机房,离首尔最近;1 GB 磁盘足够用很多年。

### A4. 部署

```bash
fly deploy
```

第一次会在 Fly 的服务器上构建镜像(约 3–5 分钟),之后每次改完代码再跑一遍这条命令就是更新。看到 `1 machines ... [app] role:started` 就成功了。

### A5. 打开、首次设置

```bash
fly open
```

浏览器会打开 `https://你的应用名.fly.dev`,进入「首次设置」创建管理员。之后在「人员」页给成员开账号,把网址 + 用户名 + 初始密码发给他们。

**验收**:用手机流量(不连 Wi-Fi)登录成员账号 → 填空闲 → 管理员求解、发布 → 手机上「添加到日历」→ 日历里出现排练。

### A6. 日常运维(记住这四条就够)

| 想做什么 | 命令 |
|---|---|
| 看服务日志 | `fly logs` |
| 更新代码后重新部署 | `fly deploy` |
| 把数据库备份拉回电脑 | `fly ssh sftp get /data/app.db ./backups/app-$(date +%Y%m%d).db` |
| 进容器里手动备份 / 灌演示数据 | `fly ssh console`,然后 `python scripts/backup.py` |

服务本身每天自动在 `/data/backups/` 里留一份备份(保留 14 天);**建议每次排练季开始前后各拉一次备份到自己电脑**。

### A7. 费用控制

- `fly.toml` 里 `auto_stop_machines = "off"` 让服务常驻(定时提醒、日历订阅都需要);这是每月约 $3 的来源。
- 排练季结束可以停机不计费:`fly scale count 0`;下次要用 `fly scale count 1`(数据在磁盘上不会丢,但磁盘本身每月约 $0.08)。
- 账单在 <https://fly.io/dashboard> 查看。

---

## 方案 B · 自己的云主机(Oracle Always Free 或任意 VPS)

前提:一台能从公网访问的 Linux 主机(Ubuntu 22.04 / 24.04),开放 80 和 443 端口。以 Oracle 为例:

### B1. 申请主机

1. <https://www.oracle.com/cloud/free/> 注册(需信用卡验证,可能排队或失败)。
2. 创建实例:Ubuntu 镜像,Always Free 形状(Ampere A1 1–2 OCPU / 6–12 GB 均可),下载 SSH 私钥。
3. 在实例的「安全列表 / 网络安全组」里放行 TCP 80、443。
4. **注意**:Oracle 会回收连续 7 天低负载的免费实例,本项目大部分时间是空闲的——务必做异地备份(B6)。

### B2. 域名

Caddy 自动申请 HTTPS 证书需要一个域名指向主机的公网 IP。没有域名可用免费的 DuckDNS:<https://www.duckdns.org> 登录 → 建一个子域名(如 `season-dance.duckdns.org`)→ 填上主机 IP。

### B3. 登录主机并安装 Docker

```bash
ssh -i ~/Downloads/你的私钥.key ubuntu@主机公网IP
```

在主机上:

```bash
curl -fsSL https://get.docker.com | sudo sh && sudo usermod -aG docker $USER && newgrp docker
```

### B4. 拉代码、写配置

```bash
git clone https://github.com/chuchu1214w/rehearsal-scheduler.git && cd rehearsal-scheduler
```

```bash
cp .env.example .env && nano .env
```

在 `.env` 里至少写这两行(域名换成你的):

```
PUBLIC_BASE_URL=https://season-dance.duckdns.org
COOKIE_SECURE=true
```

### B5. 启动

```bash
DOMAIN=season-dance.duckdns.org docker compose -f deploy/docker-compose.yml up -d --build
```

第一次构建约 5–10 分钟(ARM 主机上编译依赖会慢一些)。之后打开 `https://season-dance.duckdns.org` 做首次设置。更新代码:`git pull` 后再跑一遍这条命令。

### B6. 备份与恢复

- 自动备份在主机的 `deploy/data/backups/`(保留 14 天)。
- 拉回电脑(在你的 Mac 上执行):

```bash
scp -i ~/Downloads/你的私钥.key ubuntu@主机公网IP:rehearsal-scheduler/deploy/data/app.db ./backups/app-$(date +%Y%m%d).db
```

- 恢复:停掉服务,把备份文件覆盖回 `deploy/data/app.db`,再 `docker compose ... up -d`。
- 主机被回收时:重新申请一台 → B3–B5 → 把最近的备份覆盖回去。

---

## 常见问题

**手机日历订阅不成功?** 检查 `PUBLIC_BASE_URL` 是不是你实际访问的 https 地址;改了之后要重新部署,成员在「账号」页重新「添加到日历」。

**求解很慢?** 云主机 CPU 弱。`SOLVER_WORKERS` 默认 2;每阶段时限在演出设置的「更多设置」里(默认 90 秒)。14 人 12 曲实测几秒到十几秒。

**想换回本机?** 拉一份备份回来放到 `data/app.db`,`./scripts/start.sh` 即可,数据库结构会自动迁移。

**忘了管理员密码?** 目前没有找回功能。进容器(`fly ssh console` 或主机上 `docker compose exec app sh`)后用 Python 重置:

```bash
python -c "from app.db import make_engine, make_session_factory; from app.models import User; from app.security import hash_password; from app.config import Settings; e=make_engine(Settings.from_env().database_url); db=make_session_factory(e)(); u=db.query(User).filter_by(role='admin').first(); u.password_hash=hash_password('NewPass123'); db.commit(); print('ok', u.username)"
```

## 上线检查清单

- [ ] `PUBLIC_BASE_URL` 是 https 且与实际访问地址一致;`COOKIE_SECURE=true`
- [ ] 首次设置已完成,管理员密码不是弱密码
- [ ] 手机用流量完成:登录 → 填空闲 → 收到「排练表已发布」通知 → 添加到日历
- [ ] 拉过一次备份到自己电脑,并确认文件能用 `sqlite3`/DB Browser 打开
- [ ] 知道怎么看日志(`fly logs` / `docker compose logs -f app`)
